#include "interface.h"

int main(){
//	Matrix M;
//	float* answer = new float [M.GetA().second];
//
//	answer = sim(M);
//	for (int i = 0; i < M.GetA().second; i++){
//		std::cout << std::fixed << std::setprecision(8) << answer[i] << std::endl;
//	}
	interface();
	return 0;
}
